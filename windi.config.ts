import { defineConfig } from "windicss/helpers";

export default defineConfig({
  preflight: false,
  theme: {
    fontFamily: {
      serif: ["Bookerly Display", "serif"],
      sans: ["Bookerly", "sans-serif"],
    },
  },
});
